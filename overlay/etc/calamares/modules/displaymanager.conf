displaymanagers:
  - lightdm

# Verified directly against the actual shipped xfce4-session package:
# /usr/share/xsessions/xfce.desktop has Exec=startxfce4 and DesktopNames=XFCE.
defaultDesktopEnvironment:
    executable: "startxfce4"
    desktopFile: "xfce"

basicSetup: false
sysconfigSetup: false
