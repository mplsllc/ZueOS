---
# Matches this project's own casper layout exactly (casper/filesystem.squashfs
# under the live medium, mounted at /cdrom by casper at boot) — no change
# needed from the reference config.
unpack:
    -   source: "/cdrom/casper/filesystem.squashfs"
        sourcefs: "squashfs"
        destination: ""
